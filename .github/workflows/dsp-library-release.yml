name: DSP Library Release

on:
  push:
    tags:
      - 'dsp-v*'
  workflow_dispatch:

concurrency:
  group: dsp-library-release
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  preflight:
    name: Release preflight
    if: github.repository == 'Frieve-A/effetune' && startsWith(github.ref, 'refs/tags/dsp-v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
      - name: Verify release tag, manifests, identity, paths, and workflow syntax
        run: |
          python - <<'PY'
          import json
          import os
          import re
          import tomllib
          from pathlib import Path

          python_data = tomllib.loads(
              Path("dsp/bindings/python/pyproject.toml").read_text("utf-8")
          )
          npm_data = json.loads(
              Path("dsp/bindings/js/package.json").read_text("utf-8")
          )
          required = [
              "dsp/bindings/acceptance/python_golden_runner.py",
              "tools/verify-dsp-library-goldens.mjs",
              "js/audio/dsp-engine-binding.js",
              "js/audio/dsp-wasm-loader.js",
              "js/audio/dsp-params.generated.js",
              "js/ir-library/ir-asset-payload.js",
              "js/ir-library/ir-plugin-contract.js",
              "plugins/dsp/effetune-dsp.wasm",
              "plugins/dsp/effetune-dsp.simd.wasm",
              "plugins/dsp/effetune-dsp.meta.json",
          ]
          missing = [item for item in required if not Path(item).is_file()]
          if missing:
              raise SystemExit(f"missing DSP library inputs: {missing}")
          python_version = python_data["project"]["version"]
          npm_version = npm_data["version"]
          tag = os.environ["GITHUB_REF_NAME"]
          if python_data["project"]["name"] != "effetune":
              raise SystemExit("unexpected Python package name")
          if npm_data["name"] != "@effetune/dsp":
              raise SystemExit("unexpected npm package name")
          expected_urls = {
              "Homepage": "https://effetune.frieve.com/dsp/",
              "Documentation": "https://effetune.frieve.com/dsp/",
              "Repository": "https://github.com/Frieve-A/effetune",
              "Issues": "https://github.com/Frieve-A/effetune/issues",
          }
          if python_data["project"].get("urls") != expected_urls:
              raise SystemExit("unexpected Python project URLs")
          if (
              npm_data.get("homepage") != expected_urls["Homepage"]
              or npm_data.get("repository", {}).get("url")
              != expected_urls["Repository"]
              or npm_data.get("bugs", {}).get("url") != expected_urls["Issues"]
          ):
              raise SystemExit("unexpected npm project URLs")
          expected_files = [
              "dist/",
              "LICENSE",
              "README.md",
              "THIRD_PARTY_NOTICES.txt",
          ]
          if npm_data.get("files") != expected_files:
              raise SystemExit("npm package allowlist changed")
          if not re.fullmatch(r"dsp-v[0-9]+\.[0-9]+\.[0-9]+", tag):
              raise SystemExit(f"invalid DSP release tag: {tag}")
          if python_version != npm_version:
              raise SystemExit(
                  f"Python/npm version mismatch: {python_version} != {npm_version}"
              )
          if tag != f"dsp-v{python_version}":
              raise SystemExit(
                  f"release tag/version mismatch: {tag} != dsp-v{python_version}"
              )
          PY
          ruby -e "require 'yaml'; ARGV.each { |path| YAML.parse_file(path) }" \
            .github/workflows/dsp-library-ci.yml \
            .github/workflows/dsp-library-release.yml
      - name: Check C++ formatting
        run: >-
          find dsp -path dsp/vendor -prune -o
          \( -name '*.cpp' -o -name '*.h' \) -print0 |
          xargs -0 clang-format --dry-run --Werror
      - name: Verify generated contract
        run: |
          node scripts/gen-dsp-library-bindings.mjs --check
      - run: node examples/dsp-library/verify-site-inputs.mjs
      - run: node examples/dsp-library/generate-docs.mjs --check
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Run common verification
        run: npm run verify

  linux:
    name: Linux release candidates
    needs: preflight
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Setup Emscripten SDK
        uses: emscripten-core/setup-emsdk@4528d102f7230f0e7b276855c01ea1159be0e984 # v16
        with:
          version: 6.0.2
      - name: Verify committed DSP artifacts
        run: npm run build:dsp -- --check
      - name: Run WebAssembly parity
        run: npm run test:dsp:parity
      - name: Run Debug native tests and allocation guard
        run: |
          npm run test:dsp -- --native-build-type=Debug
          node tools/dsp-parity/run.mjs --native --allocations
      - name: Run Release native tests and parity
        run: |
          npm run test:dsp -- --native-build-type=Release
          node tools/dsp-parity/run.mjs --native
      - name: Test and pack npm candidate
        working-directory: dsp/bindings/js
        run: |
          npm install --ignore-scripts --package-lock=false
          npm test
          npm run pack:check
          mkdir -p "$RUNNER_TEMP/npm-dist"
          npm pack --pack-destination "$RUNNER_TEMP/npm-dist"
      - name: Clean-install and render from the npm candidate
        env:
          PACKAGE_DIRECTORY: ${{ runner.temp }}/npm-dist
        run: |
          package_file="$(find "$PACKAGE_DIRECTORY" -maxdepth 1 -name '*.tgz' -print -quit)"
          test -n "$package_file"
          mkdir "$RUNNER_TEMP/npm-smoke"
          cd "$RUNNER_TEMP/npm-smoke"
          npm init --yes
          npm install "$package_file"
          node --input-type=module <<'NODE'
          import assert from 'node:assert/strict';
          import { createChain } from '@effetune/dsp';

          const preset = {
            version: 1,
            chain: [{
              id: 'smoke-compressor',
              type: 'Compressor',
              enabled: true,
              channel: 'stereo',
              parameters: { threshold: -30, ratio: 8 }
            }]
          };
          const input = [
            Float32Array.from({ length: 512 }, (_, index) => Math.sin(index * 0.1) * 0.8),
            Float32Array.from({ length: 512 }, (_, index) => Math.cos(index * 0.1) * 0.8)
          ];
          const chain = await createChain(preset);
          const output = await chain.process(input, { sampleRate: 48000, seed: 42 });
          assert.equal(output.length, 2);
          assert.equal(output[0].length, 512);
          assert.notDeepEqual([...output[0]], [...input[0]]);
          chain.close();
          NODE
      - name: Verify reproducible demo
        run: node examples/dsp-library/build.mjs --check
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: ${{ runner.temp }}/npm-dist/*.tgz
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: npm-package
          path: ${{ runner.temp }}/npm-dist/*.tgz
          if-no-files-found: error
      - name: Build CPython 3.12 stable-ABI wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp312-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_CONFIG_SETTINGS: >-
            cmake.define.ET_PYTHON_STABLE_ABI=ON
            wheel.py-api=cp312
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp312
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.13'
      - name: Verify CPython 3.12 stable-ABI wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp312").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp312-abi3" in wheel.name for wheel in wheels)
          PY
          python dsp/bindings/python/tools/verify_abi3_wheel.py wheelhouse/cp312
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp312/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-ubuntu-latest-cp312
          path: wheelhouse/cp312/*.whl
          if-no-files-found: error
      - name: Build CPython 3.10 wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp310-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp310
      - name: Verify CPython 3.10 wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp310").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp310-cp310" in wheel.name for wheel in wheels)
          PY
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp310/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-ubuntu-latest-cp310
          path: wheelhouse/cp310/*.whl
          if-no-files-found: error
      - name: Build CPython 3.11 wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp311-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp311
      - name: Verify CPython 3.11 wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp311").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp311-cp311" in wheel.name for wheel in wheels)
          PY
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp311/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-ubuntu-latest-cp311
          path: wheelhouse/cp311/*.whl
          if-no-files-found: error

  windows:
    name: Windows release candidates
    needs: linux
    runs-on: windows-latest
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Run Debug native tests and allocation guard
        run: |
          npm run test:dsp -- --native-build-type=Debug
          node tools/dsp-parity/run.mjs --native --allocations
      - name: Run Release native tests and parity
        run: |
          npm run test:dsp -- --native-build-type=Release
          node tools/dsp-parity/run.mjs --native
      - name: Build CPython 3.12 stable-ABI wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp312-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_CONFIG_SETTINGS: >-
            cmake.define.ET_PYTHON_STABLE_ABI=ON
            wheel.py-api=cp312
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp312
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.13'
      - name: Verify CPython 3.12 stable-ABI wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp312").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp312-abi3" in wheel.name for wheel in wheels)
          PY
          python dsp/bindings/python/tools/verify_abi3_wheel.py wheelhouse/cp312
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp312/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-windows-latest-cp312
          path: wheelhouse/cp312/*.whl
          if-no-files-found: error
      - name: Build CPython 3.10 wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp310-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp310
      - name: Verify CPython 3.10 wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp310").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp310-cp310" in wheel.name for wheel in wheels)
          PY
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp310/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-windows-latest-cp310
          path: wheelhouse/cp310/*.whl
          if-no-files-found: error
      - name: Build CPython 3.11 wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp311-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp311
      - name: Verify CPython 3.11 wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp311").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp311-cp311" in wheel.name for wheel in wheels)
          PY
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp311/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-windows-latest-cp311
          path: wheelhouse/cp311/*.whl
          if-no-files-found: error

  macos-arm:
    name: macOS Apple Silicon release candidates
    needs: linux
    runs-on: macos-15
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build CPython 3.12 stable-ABI wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp312-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_CONFIG_SETTINGS: >-
            cmake.define.ET_PYTHON_STABLE_ABI=ON
            wheel.py-api=cp312
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp312
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.13'
      - name: Verify CPython 3.12 stable-ABI wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp312").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp312-abi3" in wheel.name for wheel in wheels)
          PY
          python dsp/bindings/python/tools/verify_abi3_wheel.py wheelhouse/cp312
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp312/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-macos-15-cp312
          path: wheelhouse/cp312/*.whl
          if-no-files-found: error
      - name: Build CPython 3.10 wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp310-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp310
      - name: Verify CPython 3.10 wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp310").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp310-cp310" in wheel.name for wheel in wheels)
          PY
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp310/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-macos-15-cp310
          path: wheelhouse/cp310/*.whl
          if-no-files-found: error
      - name: Build CPython 3.11 wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp311-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp311
      - name: Verify CPython 3.11 wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp311").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp311-cp311" in wheel.name for wheel in wheels)
          PY
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp311/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-macos-15-cp311
          path: wheelhouse/cp311/*.whl
          if-no-files-found: error

  macos-intel:
    name: macOS Intel release candidates
    needs: linux
    runs-on: macos-15-intel
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build CPython 3.12 stable-ABI wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp312-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_CONFIG_SETTINGS: >-
            cmake.define.ET_PYTHON_STABLE_ABI=ON
            wheel.py-api=cp312
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp312
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.13'
      - name: Verify CPython 3.12 stable-ABI wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp312").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp312-abi3" in wheel.name for wheel in wheels)
          PY
          python dsp/bindings/python/tools/verify_abi3_wheel.py wheelhouse/cp312
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp312/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-macos-15-intel-cp312
          path: wheelhouse/cp312/*.whl
          if-no-files-found: error
      - name: Build CPython 3.10 wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp310-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp310
      - name: Verify CPython 3.10 wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp310").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp310-cp310" in wheel.name for wheel in wheels)
          PY
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp310/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-macos-15-intel-cp310
          path: wheelhouse/cp310/*.whl
          if-no-files-found: error
      - name: Build CPython 3.11 wheel
        uses: pypa/cibuildwheel@e090b81e30c4d855ea63bf4b6e59204c09a101ae # v4.2.1
        env:
          CIBW_BUILD: cp311-*
          CIBW_ARCHS: auto64
          CIBW_SKIP: "*-musllinux_*"
          CIBW_TEST_REQUIRES: numpy>=1.26 soundfile>=0.12
          CIBW_TEST_COMMAND: >-
            python -m unittest discover
            -s {project}/dsp/bindings/python/tests -v &&
            python {project}/dsp/bindings/python/tools/audit_native_exports.py &&
            python {project}/dsp/bindings/acceptance/python_golden_runner.py
            --repo-root {project}
        with:
          package-dir: dsp/bindings/python
          output-dir: wheelhouse/cp311
      - name: Verify CPython 3.11 wheel
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = list(Path("wheelhouse/cp311").glob("*.whl"))
          assert wheels, "cibuildwheel did not produce a wheel"
          assert all("cp311-cp311" in wheel.name for wheel in wheels)
          PY
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: wheelhouse/cp311/*.whl
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-macos-15-intel-cp311
          path: wheelhouse/cp311/*.whl
          if-no-files-found: error

  acceptance:
    name: Candidate wrapper acceptance
    needs: [windows, macos-arm, macos-intel]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: python-ubuntu-latest-cp312
          path: candidate/python
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: npm-package
          path: candidate/npm
      - name: Install candidate wheel and browser harness
        run: |
          wheel="$(find candidate/python -maxdepth 1 -name '*.whl' -print -quit)"
          test -n "$wheel"
          python -m pip install "$wheel"
          npm ci --ignore-scripts
          npx playwright install --with-deps chromium
      - name: Stage the candidate npm tarball for acceptance
        run: |
          package_file="$(find candidate/npm -maxdepth 1 -name '*.tgz' -print -quit)"
          test -n "$package_file"
          mkdir -p "$RUNNER_TEMP/npm-docs-candidate"
          cd "$RUNNER_TEMP/npm-docs-candidate"
          npm init --yes
          npm install --ignore-scripts --no-audit --no-fund "$GITHUB_WORKSPACE/$package_file"
          cd "$GITHUB_WORKSPACE"
          mkdir -p "$RUNNER_TEMP/npm-candidate"
          tar -xzf "$package_file" -C "$RUNNER_TEMP/npm-candidate"
          candidate="$RUNNER_TEMP/npm-candidate/package/dist"
          test -f "$candidate/index.js"
          rm -rf dsp/bindings/js/src
          cp -R "$candidate" dsp/bindings/js/src
          cp "$candidate/internal/dsp-engine-binding.js" js/audio/dsp-engine-binding.js
          cp "$candidate/internal/dsp-wasm-loader.js" js/audio/dsp-wasm-loader.js
          cp "$candidate/internal/dsp-params.generated.js" js/audio/dsp-params.generated.js
          cp "$candidate/internal/ir-asset-payload.js" js/ir-library/ir-asset-payload.js
          cp "$candidate/internal/ir-plugin-contract.js" js/ir-library/ir-plugin-contract.js
          cp "$candidate/assets/effetune-dsp.wasm" plugins/dsp/effetune-dsp.wasm
          cp "$candidate/assets/effetune-dsp.simd.wasm" plugins/dsp/effetune-dsp.simd.wasm
          cp "$candidate/assets/effetune-dsp.meta.json" plugins/dsp/effetune-dsp.meta.json
          cp "$candidate/catalog/effects-v1.json" dsp/bindings/generated/effects-v1.json
      - name: Run published documentation against clean candidates
        run: |
          wheel="$(find candidate/python -maxdepth 1 -name '*.whl' -print -quit)"
          test -n "$wheel"
          node examples/dsp-library/verify-docs-acceptance.mjs \
            --python python \
            --npm-root "$RUNNER_TEMP/npm-docs-candidate" \
            --wheel "$wheel"
      - name: Verify frozen wrapper matrix
        run: >-
          node tools/verify-dsp-library-goldens.mjs
          --python python
          --summary .tmp/dsp-library-goldens-release.json
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: dsp-library-acceptance-summary
          path: .tmp/dsp-library-goldens-release.json
          if-no-files-found: warn

  publication-candidate:
    name: Assemble signed publication candidate
    needs: acceptance
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: python-*
          path: release/python
          merge-multiple: true
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: npm-package
          path: release/npm
      - name: Generate SPDX SBOM
        uses: anchore/sbom-action@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26 # v0.24.2
        with:
          path: release
          format: spdx-json
          output-file: release/SBOM.spdx.json
          upload-artifact: false
      - name: Assemble and verify the public release layout
        working-directory: release
        run: |
          mkdir public-assets
          cp python/*.whl npm/*.tgz SBOM.spdx.json public-assets/
          cd public-assets
          sha256sum *.whl *.tgz SBOM.spdx.json > SHA256SUMS
          sha256sum --check SHA256SUMS
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: release/public-assets/*
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ github.ref_name }}-publication-candidate
          path: release
          if-no-files-found: error

  publish-npm:
    name: Publish npm with trusted publishing
    needs: publication-candidate
    if: github.repository == 'Frieve-A/effetune' && startsWith(github.ref, 'refs/tags/dsp-v')
    runs-on: ubuntu-latest
    environment:
      name: npm
      url: https://www.npmjs.com/package/@effetune/dsp
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ github.ref_name }}-publication-candidate
          path: release
      - run: npm publish release/npm/*.tgz --access public

  publish-pypi:
    name: Publish PyPI with trusted publishing
    needs: publish-npm
    if: github.repository == 'Frieve-A/effetune' && startsWith(github.ref, 'refs/tags/dsp-v')
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/effetune/
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ github.ref_name }}-publication-candidate
          path: release
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # release/v1
        with:
          packages-dir: release/python

  github-release:
    name: Publish signed GitHub release assets
    needs: publish-pypi
    if: github.repository == 'Frieve-A/effetune' && startsWith(github.ref, 'refs/tags/dsp-v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ github.ref_name }}-publication-candidate
          path: release
      - name: Create immutable release from the existing tag
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --verify-tag \
            --generate-notes \
            --title "EffeTune DSP ${GITHUB_REF_NAME#dsp-v}" \
            release/public-assets/*
