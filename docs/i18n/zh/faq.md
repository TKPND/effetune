---
title: "常见问题与故障排除 - EffeTune"
description: "Frieve EffeTune 音频处理器的常见问题和故障排除指南。"
lang: zh
---

# EffeTune 常见问题解答（FAQ）

EffeTune 是一款面向音频爱好者的实时 DSP 应用程序，提供网页版和桌面版。本文档涵盖了设置、故障排除、多通道使用、效果操作和频率校正等内容。

如需处理 Chrome 或 Edge 标签页的音频，请参阅[浏览器扩展指南](browser-extension.md)。

## 目录
1. 流媒体初始设置
   1.1. 安装 VB-CABLE，以及按需使用 96 kHz 消除混叠
   1.2. 流媒体服务输入（以 Spotify 为例）
   1.3. EffeTune 音频设置
   1.4. 操作检查
2. 故障排除
   2.1. 音频播放质量
   2.2. CPU 使用率
   2.3. 回声
   2.4. 输入、输出或效果问题
   2.5. 多通道输出不匹配
3. 多通道和硬件连接
   3.1. HDMI + AV 接收器
   3.2. 无多通道驱动的接口
   3.3. 通道延迟和时间对齐
   3.4. 16通道限制和扩展
4. 常见问题
5. 频率响应和房间校正
6. 效果操作技巧
7. 参考链接

---

## 1. 流媒体初始设置

Windows 示例：Spotify → VB-CABLE → EffeTune → DAC/放大器。其他服务和操作系统的概念类似。

### 1.1. 安装 VB-CABLE，以及按需使用 96 kHz 消除混叠
下载 VB-CABLE Driver Pack45，以管理员身份运行 `VBCABLE_Setup_x64.exe` 并重启。如果安装后操作系统默认输出变成了 **CABLE Input**，请改回扬声器或 DAC。

如果使用 VB-CABLE 时出现 20 kHz 以上的混叠噪声，原因是其内部以 48 kHz 运行。仅针对这个问题，将 **CABLE Input** 和 **CABLE Output** 都设置为 24 位、96,000 Hz。然后以管理员身份启动 `VBCABLE_ControlPanel.exe`，选择 **Menu▸Internal Sample Rate = 96000 Hz**，再点击 **Restart Audio Engine**。需要以管理员身份更改，才能在重启后保留该设置。这会更改 VB-CABLE 本身，EffeTune 以 96 kHz 运行时通常不需要这样设置。

### 1.2. 流媒体服务路由（以 Spotify 为例）
打开 **设置▸系统▸声音▸音量混合器**，将 `Spotify.exe` 输出设置为 **CABLE Input**。播放一首曲目以确认扬声器没有声音。
在 macOS 上，可使用 Rogue Amoeba 的 **SoundSource** 将 Spotify 输出同样设置为 **CABLE Input**。

### 1.3. EffeTune 音频设置
启动桌面应用并打开 **配置音频**。
- **输入设备：** CABLE Output (VB-Audio Virtual Cable)
- **输出设备：** 物理 DAC/扬声器
- **采样率：** 选择 96 kHz。这是 EffeTune 的内部处理率，通常无需更改操作系统、音频设备或 VB-CABLE 的采样率。这样可以减少抗混叠处理不足的非线性效果器产生的、落入可听频段的混叠噪声。请确认应用中显示的实际采样率。如果出现丢音，请先减少高负载或已启用的效果器，必要时再降低采样率。此设置与上面的 VB-CABLE 专用处理无关。

### 1.4. 操作检查
在 Spotify 播放时，切换 EffeTune 中的主 **ON/OFF** 并确认声音发生变化。

---

## 2. 故障排除

### 2.1. 音频播放质量问题

| 症状 | 解决方案 |
| ------ | ------ |
| 音频中断或故障 | 点击网页应用左上角的 **重置音频** 按钮或在桌面应用的 **视图** 菜单中选择 **重新加载**。如有必要，减少活跃效果的数量。 |
| 失真或剪切 | 在效果链末端插入 **Level Meter** 并保持电平低于 0 dBFS。如果需要，在 Level Meter 之前添加 **Brickwall Limiter**。 |
| 使用 VB-CABLE 时在 20 kHz 以上出现混叠 | 内部采样率可能仍为 48 kHz。请按 1.1 节的可选 96 kHz 步骤设置。 |

### 2.2. CPU 使用率高
禁用不使用的效果或从 **效果管道** 中移除它们。

### 2.3. 回声
您的输入和输出设备可能形成回路。确保 EffeTune 的输出不会返回到其输入。

### 2.4. 输入、输出或效果问题

| 症状 | 解决方案 |
| ------ | ------ |
| 无音频输入 | 确保播放器输出到 **CABLE Input**。在浏览器中允许麦克风权限并选择 **CABLE Output** 作为输入设备。 |
| 效果不工作 | 确认主开关、每个效果和任何 **Section** 都处于 **ON** 状态。如有必要，重置参数。 |
| 无音频输出 | 对于网页应用，检查操作系统和浏览器输出是否指向您的 DAC/放大器。对于桌面应用，检查 **配置音频** 中的输出设备。 |
| 其他播放器报告"CABLE Input 正在使用中" | 确保没有其他应用程序正在使用 **CABLE Input**。 |

### 2.5. 多通道输出不匹配
EffeTune 按通道编号顺序输出，最多支持 16 个通道。请让 **Output Channels** 与设备配置一致。对于 7.1ch 设置，请将设备和 EffeTune 都设为 8ch，并按设备显示的通道标签路由后置音频。对于 16 通道设置，请在两处都选择 16 通道并确认设备的通道映射。

---

## 3. 多通道和硬件连接

### 3.1. HDMI + AV 接收器
将 PC 的 HDMI 输出设置为与 AV 接收器的扬声器布局相匹配。PC、接收器和 HDMI 连接均支持时，EffeTune 可以通过单根电缆发送多达 16 个通道。较旧的接收器可能会降低音质或意外重新映射通道。

### 3.2. 无多通道驱动的接口（例如 MOTU M4）
Out 1‑2 和 Out 3‑4 显示为独立设备，无法实现 4 通道输出。解决方法：
- 使用 **Voicemeeter** 通过 ASIO 合并通道。
- 使用 **ASIO Link Pro** 创建一个虚拟 4 通道设备（高级）。

### 3.3. 通道延迟和时间对齐
使用 **MultiChannel Panel** 或 **Time Alignment** 以 10 微秒为步长延迟通道（最小 1 个样本）。当前置扬声器需要与实测延迟大得多的 Bluetooth 或无线后置扬声器对齐时，可将前置通道延迟 100-400 毫秒；这并非常规扬声器距离校正的典型数值。视频同步必须在播放器端调整。

### 3.4. 16通道限制和扩展
EffeTune 目前最多支持 16 个输出通道。

---

## 4. 常见问题

| 问题 | 回答 |
| ------ | ------ |
| PWA 版支持哪些设备？ | 可在 Android 手机/平板、iPhone/iPad、Windows、macOS、Linux、ChromeOS 等主要移动端和桌面环境中使用。PWA 不是针对某一设备的原生应用，而是在浏览器中运行；安装方式、音频输入/输出设备选择以及可播放的音乐格式取决于浏览器和操作系统。 |
| 无法安装 PWA 版怎么办？ | 请使用 EffeTune 网站上的 **安装 PWA 版** 按钮，或在网页版右上角的齿轮菜单中选择 **安装应用**。如果没有显示安装项，在 Android 或 PC 上请用 Chrome、Edge 等 Chromium 浏览器打开。iPhone/iPad 请用 Safari 打开，并通过分享菜单添加到主屏幕。应用内浏览器、无痕/隐私浏览模式或旧版浏览器可能不会显示安装选项。 |
| 环绕声输入（5.1ch 等）？ | Web Audio API 将输入限制为 2 个通道。输出和效果支持最多 16 个通道。 |
| 推荐的效果链长度？ | 使用 CPU 允许的尽可能多的效果，同时不造成音频中断或高延迟。 |
| 如何获得最佳音质？ | 将 EffeTune 的**采样率**设为 96 kHz，从轻微的效果器设置开始，并用 **Level Meter** 监控余量。如果出现丢音，请先减少高负载或已启用的效果器，必要时再降低采样率。需要时可添加 **Brickwall Limiter**。 |
| 它适用于任何音源吗？ | 是的。借助虚拟音频设备，您可以处理流媒体、本地文件或物理设备的音频。 |
| EffeTune 可以播放受 DRM（数字版权管理）保护的内容吗？ | 无法直接播放。EffeTune 用于处理音频，而受版权保护的内容通常只允许在内容提供商授权的环境中播放，因此第三方音频处理应用可能无法使用。请通过提供商的官方应用或网站播放，并遵守其服务条款及支持的音频输出方式。EffeTune 不会移除或绕过内容保护。 |
| 可以在没有音频输入的情况下只使用音乐文件播放器吗？ | 可以。启动后如果麦克风声音会从耳机等输出中听到，请在 **音频设置** 的 **输入设备：** 中选择 **无（仅音乐文件播放器）**。EffeTune 会用静音源保持音频管道运行，因此内置播放器以及 **Oscillator** 等信号生成效果仍可工作。选择音频输入时，可以连接 USB 音频接口等处理外部设备的声音，也可以用 **Spectrum Analyzer** 等查看输入信号。 |
| 移动端网页应用可以处理其他应用的声音吗？ | 通常不能。移动端浏览器一般不提供可接收其他应用声音的通用回环输入，因此移动端主要适合使用 EffeTune 内置播放器。 |
| 支持哪些音乐文件格式？ | 取决于浏览器和操作系统的音频解码能力。通常 MP3、WAV、AAC/M4A 在多数环境中较容易播放；FLAC、OGG/Vorbis、Opus/WebM 等格式的支持因环境而异。EffeTune 也可以只播放 MP4 文件中的音轨而不显示视频；能否播放取决于文件内部的音频编解码器，其中 AAC 是最常见的兼容选择。无法播放时，请尝试 MP3、AAC/M4A 或 WAV。 |
| 可以一次打开多个音乐文件吗？ | 可以。点击 **打开音乐文件** 后，在设备自带的文件选择界面中选择多个文件再打开，它们会作为播放列表载入。是否支持多选或一次选中文件夹内所有文件，取决于设备、浏览器和文件选择界面。 |
| 音乐库有什么作用？ | 它会为选中的音乐文件夹建立索引，让您按曲目、专辑、艺人、流派或曲目文件直接所在的子文件夹浏览和搜索，并在 EffeTune 中播放结果。库元数据和播放列表保存在应用内，不会写入音频文件。 |
| 音乐库可在哪些环境中使用？ | 桌面应用提供完整的文件夹扫描。Chromium 浏览器在可用时使用 File System Access。Safari 和 Firefox 使用导入式后备方案，因此刷新页面或权限失效后，可能需要重新选择文件夹或文件。 |
| 如何刷新或重新连接音乐库文件夹？ | 添加、删除或编辑文件后，请使用 **重新扫描**。如果某个文件夹显示访问权限缺失，请点击该文件夹的 **重新连接** 按钮，并重新授予同一文件夹的访问权限。 |
| 音乐库可以导入或导出哪些播放列表格式？ | 音乐库可以导入 M3U、M3U8、PLS 和 XSPF 播放列表，并可导出 M3U8 或 XSPF 播放列表。 |
| 音乐库会修改我的音频文件吗？ | 不会。扫描、读取元数据、缓存封面、编辑播放列表和播放操作都只在应用内进行，绝不会修改磁盘上的音频文件。 |
| 网页应用无法选择输出设备怎么办？ | 取决于浏览器支持和权限。请在 Chrome/Chromium 的安全页面中尝试，或将要使用的 DAC/功放设为操作系统或浏览器的默认输出。 |
| 为什么 **采样率：** 或 **输出通道：** 不是指定值？ | 设置画面将 96 kHz 显示为采样率的默认值，但首次保存设置前，应用可能按操作系统或浏览器的默认值启动。在网页版中，如果浏览器或设备不接受 96 kHz 等不支持的值，EffeTune 会改用可用的采样率。请查看应用中显示的实际采样率。输出通道的支持情况也取决于浏览器和设备。 |
| 为什么采样率和通道指示器变成了红色？ | EffeTune 检测到已启用效果器的处理未能及时完成。处理恢复正常约 10 秒后，红色警告会消失。请减少已启用的效果器数量。 |
| 网页播放器会记住播放列表吗？ | 循环/随机播放设置会保存，但普通文件选择受浏览器限制，重新加载后通常无法恢复播放列表。 |
| 移动端熄屏后还能继续播放吗？ | 取决于浏览器，尤其在 iOS 上无法保证稳定。支持的环境会使用 Wake Lock，但不能保证后台播放。 |
| EffeTune 的节能模式有何区别？ | Web/PWA 版和 Electron 桌面版均可使用这些模式。在 **设置** → **节能** 中选择。**优先后台处理** 会在外部输入静音时继续进行音频处理。**均衡节能（默认）** 通常会保留所选音频输入，同时减少静音期间的 DSP 处理和界面更新。**最大限度节能** 还可以在所选时间后停止未使用或在后台持续静音的输入。当当前路由能够确认安全时，播放位置可以继续前进，而 DSP 会被旁路或输出保持为零。应用不会显示单独的状态指示器；只有在需要用户操作时，菜单中才会出现 **恢复音频处理** 或 **恢复音频输入**。 |
| “隐藏时跳过显示类 DSP”有什么作用？ | 此选项默认开启。当图表无法显示时，EffeTune 会绕过 Analyzer 效果以降低处理负载。这包括 Effect Pipeline 未显示的情况，例如打开音乐库、移动端的播放器标签或进行 Double Blind Test 时，以及 EffeTune 已最小化、处于迷你播放器或浏览器页面被隐藏时。音频会原样通过，并在图表重新可见时恢复分析。若需要在后台继续分析，请关闭此选项。 |
| “将画面与音频同步”有什么作用？ | 它会让图表和仪表与听到的声音对齐。由于可能增加音频延迟，此选项默认关闭。浏览器扩展不支持此功能，而且 Bluetooth 等设备上的视觉效果与声音仍可能略有不同步。 |
| “静音阈值”和“在以下时间后停止音频输入”分别控制什么？ | **静音阈值**（-90 至 -20 dBFS，以 10 dB 为步进）决定输入和输出的测量功率低于何值时被视为静音；数值越低，越不容易把微弱声音判为静音。在 **最大限度节能** 中，**在以下时间后停止音频输入**（1/5/15 分钟或 **永不**）只控制麦克风等输入的释放。它与无路由音频图进入 Suspended 的较短延迟相互独立，因此可能在保留输入的同时暂停音频图。 |
| 隐藏 Web/PWA 后，“优先后台处理”能保证仍持续处理吗？ | 不能。EffeTune 会优先保持处理，并避免因外部输入静音而主动进入 Suspended，但浏览器和操作系统仍可能冻结、暂停或丢弃隐藏页面。如果 **最大限度节能** 已停止输入，仅返回页面或外部信号恢复都不会自动再次请求麦克风权限；请通过明确的用户操作选择 **恢复音频处理**。 |
| AV 接收器与音频接口的成本比较？ | 重用带 HDMI 的 AV 接收器很简单。对于以 PC 为中心的设置，多通道接口加小型放大器提供良好的成本和质量。 |
| 安装 VB-CABLE 后其他应用没有声音 | 操作系统默认输出被切换到 **CABLE Input**。在声音设置中改回来。 |
| 分割后只有 3+4 通道改变音量 | 在分配器后放置 **Volume** 效果并将 **Channel** 设置为 3+4。如果放在前面，所有通道都会改变。 |

---

### 分享与复现IR Reverb结果

URL和preset只包含IR ID，不包含音频文件。如果提示IR缺失或没有wet信号，请导入同一原始文件自动重新关联，或在 **Impulse Response Library** 中选择替代IR，并检查 **Dry** 和 **Dry Level** 设置。

要在其他环境复现结果，请将未经修改的IR文件连同URL/preset以及来源和许可证信息一起提供，同时保持sample rate、通道选择和IR Reverb设置一致。多通道send/return可用 **Matrix** 将所需通道复制到空闲bus，关闭 **Dry**、将 **Wet Level** 设为0 dB，并只把wet bus返回到目标输出。

## 5. 频率响应和房间校正

### 5.1. 将 AutoEQ 设置导入 15Band PEQ
您可以通过右上角的按钮直接导入 AutoEQ 均衡器设置。

### 5.2. 粘贴测量校正设置
从测量页面复制 5Band PEQ 设置，并使用 **Ctrl+V** 或菜单粘贴到 **效果管道** 视图中。

### 5.3. 将多通道测量用于 Room EQ
在测量页面的**输出通道**中选择要测量的各个通道，即可在一次测量会话中完成这些通道的测量。保存后，Room EQ 会在 **Measurement Ch 1**、**Measurement Ch 2** 等各通道专用列表中为每个通道显示单独的条目，例如 `测量名称 [Ch 1]`。请在每个 **Measurement Ch** 列表中选择与该输出通道对应的条目。Room EQ 不会自动分配通道。

如果要将静态的频率响应校正粘贴到**效果管道**中，请使用**复制各通道 PEQ 设置**。如果要对每个通道进行基于脉冲响应的房间校正，请使用 Room EQ。

---

## 6. 效果操作技巧
* 信号流是从上到下。
* 使用 **Matrix** 效果进行转换，如 2→4ch 或 16→2ch（在总线路由中设置 **Channel = All**）。
* 使用 **MultiChannel Panel** 管理最多 16 个通道的电平、静音和延迟。

---

## 7. 参考链接
* EffeTune 桌面版：<https://github.com/Frieve-A/effetune/releases>
* EffeTune 网页版：<https://effetune.frieve.com/effetune.html>
* 频率响应测量：<https://effetune.frieve.com/features/measurement/measurement.html>
* VB-CABLE：<https://vb-audio.com/Cable/>
* Voicemeeter：<https://vb-audio.com/Voicemeeter/>
* ASIO Link Pro（非官方修复版）：搜索 "ASIO Link Pro 2.4.1"
