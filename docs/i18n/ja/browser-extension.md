---
title: "ブラウザ拡張版 - EffeTune"
description: "EffeTune ブラウザ拡張版で最大4つのタブの音声を加工し、URL別プリセットとサンプリング周波数を設定する方法。"
lang: ja
---

# EffeTune ブラウザ拡張版

EffeTune ブラウザ拡張版は、最大4つのブラウザタブの音声を、それぞれ独立したステレオの Effect Pipeline で同時に加工します。デスクトップ版を起動したり、仮想オーディオデバイスを設定したりせずに、動画サイトや音楽サイトの音を加工して聴くときに便利です。

## 対応環境

PC の Chrome 116 以降、または対応する Chromium ベースの Microsoft Edge で使用できます。Firefox、Safari、モバイルブラウザ、プライベートブラウズには対応していません。各タブにステレオのエフェクトパイプラインを使います。

## ローカルパッケージの導入

ブラウザストアから入手した場合は、ストアでインストールしてください。ローカルパッケージの場合は、`effetune-extension-<version>.zip` を継続して保管するフォルダに展開してから、展開したフォルダを読み込みます。

1. Chrome では `chrome://extensions`、Edge では `edge://extensions` を開きます。
2. **Developer mode** をオンにします。
3. **Load unpacked** を選び、展開した拡張フォルダを選択します。

**Load unpacked** では ZIP ファイル自体は導入できません。展開フォルダ内のファイルを置き換えた後は、このページから拡張機能を再読み込みしてください。

## 加工の開始、比較、停止

1. 加工したい音声を再生するタブを開きます。
2. ブラウザのツールバーから EffeTune 拡張機能を開きます。
3. **Start on this tab** を選びます。エフェクトチェーンの準備ができると、状態表示が **Starting…** から **Processing** に変わります。

別のタブも加工するには、そのタブで拡張機能を開き、**Start on this tab** を選びます。ポップアップにはセッションの一覧が表示されます。同時に動かせるのは4つまでです。5つ目を開始する前に、いずれかを停止してください。

各タブの **Bypass** を使うと、セッションを維持したままエフェクトなしの音を確認できます。終了するときは、そのタブの **Stop** を選びます。そのタブだけが通常再生に戻り、ほかのタブの加工は続きます。

拡張機能のポップアップやエディターを閉じても、加工は続きます。再度開くと、現在の対象タブと実際の処理状態を確認できます。ブラウザを再起動した後は、新しいセッションを手動で開始してください。拡張機能が自動でタブを取得することはありません。

## Effect Pipeline の編集とプリセット

**Edit pipeline** で **EffeTune Pipeline Editor** を開き、ヘッダーで対象タブを選びます。EffeTune と同じように、エフェクトの追加、並び替え、個別エフェクトの有効・無効の切り替え、パラメーターの調整を行えます。解析表示も選択中のタブに切り替わります。動作中のセッションがないときは、**Offline pipeline** で次回開始時の既定パイプラインを編集します。

ポップアップでタブを選び、**Saved preset** と **Apply to selected tab** を使うと、エディターを開かずにそのタブのパイプライン全体を切り替えられます。エディターでは **Pipeline Presets** を開き、**Save as** でパイプライン全体のプリセットを保存できます。プリセットのインポートまたはエクスポートは、**Settings** を開いて **Import preset…** または **Export preset** を選びます。拡張機能のプリセットと保存設定は拡張機能内に保存され、ウェブ版やデスクトップ版とは自動同期しません。

プリセットに対応外のルーティング、エフェクト、または利用できない外部アセットが含まれる場合、そのプリセットは適用されず、現在のパイプラインは変更されません。

ウェブ版またはデスクトップ版の測定結果を Room EQ や Crosstalk Cancellation で使うには、測定結果をそちらで JSON としてエクスポートします。拡張機能のエディターで **Settings** を開き、**Import measurement…** を選んで、その JSON ファイルを指定してください。Crosstalk Cancellation または Room EQ の位相補正で使う場合は、インパルス応答を含めてエクスポートします。インポートした測定結果は Room EQ の **Measurement** 一覧へすぐに追加され、拡張機能のブラウザストレージに保存されます。自動同期はされません。インポートしたコピーを削除するには、その一覧で対象を選び、一覧横の **Delete** を選択します。確認後、その測定結果を使っているすべての Room EQ と Crosstalk Cancellation の割り当てが解除されてから、コピーが削除されます。

対応する保存データを選んでまとめて移行するには、エディターの **Settings > Backup / Restore** を開きます。同じ `.effetune_backup` ファイルをウェブ版やデスクトップ版でも使用でき、保存済みのパイプラインプリセット、エフェクトプリセット、インパルス応答、インポートした測定結果を含められます。拡張機能に対応していないルーティングやエフェクトを使うパイプラインも保存され、再度バックアップできますが、拡張機能では適用できません。URLルールと拡張機能のSample rate設定は含まれません。

## URL別プリセットとサンプリング周波数

**Settings** の **URL rules…** でパターンを追加し、保存済みプリセットを選んでルールを有効にします。パターンは `example.com/music/*` のように `host/path` で指定し、`*` は任意の文字列に一致します。有効なルールのうち最初に一致したものを使います。ホスト名の大文字・小文字は区別せず、scheme、クエリ文字列、フラグメントは照合しません。並べ替えで優先順位を変え、不要なルールは無効化または削除できます。

各タブの開始操作は手動です。開始時とURL変更時にプリセットが自動で選ばれ、一致するルールがなければ既定パイプラインを使います。ルールの変更は次回の開始またはページ移動から反映されます。URLルールで選ばれたパイプラインを編集すると、その保存済みプリセットを更新します。それ以外の編集は既定パイプラインを更新します。手動でプリセットを適用した後の編集も既定パイプラインに保存します。プリセットを削除すると参照するルールが無効になり、そのルールを使用中のタブは既定パイプラインに戻ります。

**Settings** の **Sample rate** は、動作中のすべてのタブに適用されます。**Auto**、**44.1 kHz**、**48 kHz**、**96 kHz**、**192 kHz** から選べます。Autoではブラウザが周波数を選びます。変更時はタブのキャプチャを維持したまま、全タブの音声処理が短時間再起動します。新しい周波数で処理できないタブは通常再生に戻ります。別の周波数を選び、そのタブを再度開始してください。

## 権限と制限

拡張機能が取得するのは、あなたが明示的に加工を開始したタブの音声だけです。ページ移動後も対象タブのURLを読み取り、保存済みプリセットを選びます。ページ内容の読み取り、スクリプトの挿入、マイクの使用、録音、外部への音声送信は行いません。

ステレオのパイプラインでは、Bus Routing で Main と Bus 1–4 を使い、エフェクトを並列に配置できます。3チャンネル以上、新しい測定の実行と機器制御、Music Library、ファイルの一括変換、デスクトップ専用のデバイス機能やファイルパスを使う機能は、拡張機能では利用できません。

保護されたコンテンツは取得できない場合があります。拡張機能はコンテンツ保護を回避しません。取得を開始できない場合、EffeTune は処理を停止し、タブは通常再生に戻ります。そのタブで音声が再生されていることを確認してから、**Start on this tab** をもう一度選んでください。

## 問題が起きたとき

- 状態が **Needs attention** のときは、選択したタブで音声が再生されていることを確認してから、**Start on this tab** をもう一度選んでください。
- タブを取得できないときは、そのタブで音声が再生されていることを確認してから、もう一度試してください。
- プリセットを適用できないとき、エディターは現在のパイプラインを保持します。プリセットを変更するか、必要なアセットを利用できるようにしてから試してください。
